generator client {
  provider   = "prisma-client-js"
  engineType = "client"
}

datasource db {
  provider = "postgresql"
}

model Report {
  id        Int      @id @default(autoincrement())
  filename  String
  insights  String
  timestamp DateTime @default(now())
}
